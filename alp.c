#include <stdio.h>

int main()
{
    char k;
    printf("Enter any character: ");
    scanf("%c", &k);
   if((k >= 'a' && ch <= 'z') || (k >= 'A' && ck <= 'Z'))
    {
        printf("Character is an ALPHABET");
    }
    else
    {
        printf("Character is not ALPHABET");
    }

    return 0;
}

